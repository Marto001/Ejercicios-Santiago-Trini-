#include <stdio.h>
#include <stdlib.h>
#include <Math.h>

int int main(int argc, char const *argv[]) {
  double x = atof(argv[3]) {;
  double y = atof(argv[4]) {;
  double distance = sqrt(x*x + y*y)
  printf("la distancia entre (0,0) y (%.2f,%.2f) es %.2f", x, y, distance )
  return 0;
}
